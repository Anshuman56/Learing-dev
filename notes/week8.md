## Day 50 — MonDay (13 july)

Time spent: 3h 10m
Energy (1–10): 5

### What I built/learned today

- I build my first server using node js. And read what is http protocol and how client and server talk to each other and how user-agent send the request to the server and get response from the server. And the web browser (user-agent) send a request object to the server and in the request object it had the methord and url and header. and the server send the status code, and setHeader for the response and if it is json it will call the write() method and if it is text/html content then data goes end() methord.

### What confused me

- Network layer, transport layer, UDP, TCP, application layer. What does these thing do and where they live?

### One thing that clicked

- How to creat a server in Node js? You first to import the http modul and then create the server by calling the http.createServer() methord and after that listen to the port. Then you can listen to url and methord from the request object.

### Tomorrow's first task

- Read expressjs.com — "Getting started" → "Installing" and "Hello world example". Then → "Basic routing".

### Stuck on / need to revisit

- Nothing
