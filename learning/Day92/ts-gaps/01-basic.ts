function add(a: number, b: number): number {
  return a + b;
}

function greet(name: string): string {
  return "hello " + name;
}

function isAdult(age: number): boolean {
  return age > 18;
}

console.log(add(4.5, 43));
console.log(greet("lag"));
console.log(isAdult(4.5));
